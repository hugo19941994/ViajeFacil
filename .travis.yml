addons:
  apt:
    packages:
    - qt5-default
    - qttools5-dev-tools

#install:
  #- sudo apt-get update
  #- sudo apt-get install qt5-default qttools5-dev-tools

script:
  - qmake -project
  - qmake ViajeFacil.pro
  - make
